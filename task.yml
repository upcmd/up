version: 1.0.0

tasks:
  -
    name: build
    desc: build binary
    task:
      -
        func: shell
        desc: build the binary to build directory
        do:
          - rm -rf ./build
          - mkdir -p ./build
          - go build -o build/up app/ups/*.go

  -
    name: test
    desc: test all the function tests
    task:
      -
        func: shell
        do:
#          - go test ./tests/functests/*.go -v | sed ''/PASS/s//$(printf "\033[32mPASS\033[0m")/'' | sed ''/FAIL/s//$(printf "\033[31mFAIL\033[0m")/''
          - go test ./tests/functests/*.go -v

