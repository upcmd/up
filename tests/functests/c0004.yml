doc_meta: |
  folder: quick-start
  weight: 16
  title: Use array
  head: |
    Mix of yaml literal and single line of shell script

  sections:
    - title: Demo
      log: yes

notes:
  goal:
    - to test the shell function can handle multiline scripts in array

tasks:
  -
    name: task
    desc: this is task1
    task:
      -
        func: shell
        desc: do step1 in shell func
        do:
          - |
             echo "hello"
             echo "world"
          -  echo "how are you"

