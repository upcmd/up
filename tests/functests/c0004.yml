notes:
  goal:
    - to test the shell function can handle multiline scripts

tasks:
  -
    name: task
    desc: this is task1
    task: #this comment will not be treated as desc of the task, removing # will invalid the yml
      -
        func: shell
        desc: do step1 in shell func
        do:
          - |
             echo "hello"
             echo "world"
