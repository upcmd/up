doc_meta: |
  folder: quick-start
  weight: 14
  title: Yaml literal
  head: A example to show using yaml string literal to execute multiple line of shell script

  sections:
    - title: Demo
      log: yes

notes:
  goal:
    - to test the shell function can handle do but without using array

tasks:
  -
    name: task
    desc: this is task1
    task:
      -
        func: shell
        desc: do step1 in shell func
        do: |
          echo "hello"
          echo "world"
