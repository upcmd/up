
version: 1.0.0
Verbose: vvv
MaxCallLayers: 8
RefDir: .
TaskFile: up
ConfigDir: .
ConfigFile: upconfig

Modules:

  - #if there is no repo, then it will use the dir as module and incorporate as module
    dir: hello-module/
