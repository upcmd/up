vars:
  folder: module
  title: moule lock
  log: yes
  weight: 2148
  head: |
    Showcase the module lock

  sections:

    - title: lock module versions
      content: |
        Please refer to the module command to lock all module versions
      filelookup: modlock.yml

    - title: config file - upconfig.yml
      filelookup: upconfig.yml
