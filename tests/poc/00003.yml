
tasks:
	job5: #a task can be a sequential steps
		- sh:
			  - echo "hello"
			  - echo "world"

		- sh: |
			  echo "{{.msg}}"
			  echo "world"

		- include: |   #a job can include a step of include which include sub steps
			  a.yml
			  ./a/b/c.yml